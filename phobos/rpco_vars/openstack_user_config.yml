---
cidr_networks:
  container: 172.20.52.0/24
  tunnel: 172.20.53.0/24
  storage: 172.20.54.0/24

used_ips:
  - "172.20.52.1,172.20.52.50"
  - "172.20.53.1,172.20.53.50"
  - "172.20.54.1,172.20.54.50"
  #- "172.29.248.1,172.29.248.50"

global_overrides:
  # The internal and external VIP should be different IPs, however they
  # do not need to be on separate networks.
  external_lb_vip_address: 172.20.41.155
  internal_lb_vip_address: 172.20.52.11
  tunnel_bridge: "br-vxlan"
  management_bridge: "br-mgmt"
  provider_networks:
    - network:
        container_bridge: "br-mgmt"
        container_type: "veth"
        container_interface: "eth1"
        ip_from_q: "container"
        type: "raw"
        group_binds:
          - all_containers
          - hosts
        is_container_address: true
        is_ssh_address: true
    - network:
        container_bridge: "br-vxlan"
        container_type: "veth"
        container_interface: "eth10"
        ip_from_q: "tunnel"
        type: "vxlan"
        range: "1:1000"
        net_name: "vxlan"
        group_binds:
          - neutron_linuxbridge_agent
    - network:
        container_bridge: "br-vlan"
        container_type: "veth"
        container_interface: "eth12"
        host_bind_override: "eth12"
        type: "flat"
        net_name: "flat"
        group_binds:
          - neutron_linuxbridge_agent
    - network:
        container_bridge: "br-vlan"
        container_type: "veth"
        container_interface: "eth11"
        type: "vlan"
        range: "1:1"
        net_name: "vlan"
        group_binds:
          - neutron_linuxbridge_agent
    - network:
        container_bridge: "br-storage"
        container_type: "veth"
        container_interface: "eth2"
        ip_from_q: "storage"
        type: "raw"
        group_binds:
          - glance_api
          - cinder_api
          - cinder_volume
          - nova_compute

###
### Infrastructure
###

# galera, memcache, rabbitmq, utility
shared-infra_hosts:
  infra1:
    ip: 172.20.52.11

# repository (apt cache, python packages, etc)
repo-infra_hosts:
  infra1:
    ip: 172.20.52.11

# load balancer
haproxy_hosts:
  infra1:
    ip: 172.20.52.11

###
### OpenStack
###

# keystone
identity_hosts:
  infra1:
    ip: 172.20.52.11

# cinder api services
storage-infra_hosts:
  infra1:
    ip: 172.20.52.11

# glance
image_hosts:
  infra1:
    ip: 172.20.52.11

# nova api, conductor, etc services
compute-infra_hosts:
  infra1:
    ip: 172.20.52.11

# heat
orchestration_hosts:
  infra1:
    ip: 172.20.52.11

# horizon
dashboard_hosts:
  infra1:
    ip: 172.20.52.11

# neutron server, agents (L3, etc)
network_hosts:
  infra1:
    ip: 172.20.52.11

# nova hypervisors
compute_hosts:
  compute1:
    ip: 172.20.52.14

# cinder storage host (LVM-backed)
storage_hosts:
  infra1:
    ip: 172.20.52.11
    container_vars:
      cinder_backends:
        limit_container_types: cinder_volume
        rbd:
          volume_driver: cinder.volume.drivers.rbd.RBDDriver
          volume_backend_name: rbd
          rbd_pool: volumes
          rbd_ceph_conf: /etc/ceph/ceph.conf
          rbd_user: cinder
          rbd_flatten_volume_from_snapshot: false
          rbd_max_clone_depth: 5
          rbd_store_chunk_size: 4
          rados_connect_timeout: -1
          rbd_secret_uuid: "{{ cinder_ceph_client_uuid }}"
